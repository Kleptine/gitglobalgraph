This folder contains a JS-based set of workflow tests. This served as a design prototype for the early system to verify that it was able to handle all suitable cases. Currently un-maintained, but useful for documentation.